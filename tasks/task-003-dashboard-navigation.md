# task-003：管理后台信息架构与页面交互

- 状态：已验证，待部署
- 工作目录：`D:\code_CPL\weekly_report_assistant`
- 交付分支：`feature/task-003-dashboard-navigation`
- 基线：`origin/main@20a93481dcb52a2d097e3033c15908208f922540`

## 目标

- 将现有单页联调控制台重构为带固定侧边栏的独立周报管理后台。
- 提供概览、周报展示、周报配置、模型配置、推送设置五个页面入口。
- 周报展示支持当前周报、正式周报和历史记录切换，并提供结构化正文编辑。
- 周报配置支持报告基础参数、项目基线及项目基础描述维护。
- 推送设置支持测试目标、正式目标、确认人、机器人回调和推送时间配置。
- 模型配置继续继承 `bi_center` 的部署配置和同一把 Key，不在前端返回明文。

## 验收条件

- 桌面端固定侧边栏，窄屏使用抽屉侧边栏；页面切换同步 URL hash，并支持刷新后恢复。
- 原有同步、生成、编辑、预览、审核、正式发送、归档、撤回、人员覆盖和回调查看能力均可访问。
- 正式发送仍需人工确认，`autoFormalSendEnabled` 继续由后端强制关闭。
- 项目基线保存后进入独立服务配置，并作为 AI 周报归纳的受限背景，不反向修改 `bi_center`。
- API Key、Token、人员明细和运行数据不进入 Git。
- Python 单测/编译、前端语法、静态契约和浏览器桌面/移动交互验证通过。

## 迁移与回滚

- 复用现有 `app_config.weekly_workflow` JSON 配置，不新增数据库表或列。
- `projectBaseline` 为新增可选配置键；旧配置缺失时自动归一化为空数组，已有数据无影响。
- 回滚代码后该 JSON 键会被旧版本忽略，不影响现有周报、推送日志和源表快照。

## 验证记录

- 2026-08-14：Python 38 项单元测试通过，`compileall`、`node --check`、`git diff --check` 通过。
- 2026-08-14：在本地真实 FastAPI 页面完成概览、周报展示、周报配置、模型配置、推送设置的浏览器导航和渲染验收；新增项目交互正常。
