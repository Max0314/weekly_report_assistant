# Task 006：配置说明与人员目录状态

## 目标

- 将侧栏品牌区改为静态产品标识，消除按钮或导航入口的视觉歧义。
- 在周报配置页展示 bi_center 只读目录的部署配置状态、缓存规模和版本。
- 为周报配置、模型配置、推送设置中的字段补充用途与填写口径。
- 验证生产 bi_center 人员目录连接并刷新本地缓存，不修改 bi_center。

## 交付信息

- 工作目录：`D:\code_CPL\weekly_report_assistant`
- 交付分支：`feature/task-006-config-guidance`
- 基线：`origin/main` @ `b9a8e87631c43a1a4a7608c734dd40f49833e2bd`
- 当前状态：开发中

## 数据与安全

- 不新增数据库表或列，不改变既有人员目录缓存结构。
- `BI_CENTER_API_TOKEN` 仍只从部署环境读取；管理接口只返回是否已配置，不返回明文。
- 人员同步仅调用 bi_center 版本化只读接口，并事务性刷新本服务 SQLite 缓存。

## 验收条件

- 侧栏品牌区无渐变卡片、阴影、悬停态或点击语义。
- 配置页能显示 bi_center 地址、Token 配置状态、人员/组织数量、目录版本和刷新时间。
- 所有可编辑配置字段旁有用途或填写说明，动态项目和群字段有字段说明区。
- Python 测试、编译检查及 `node --check static/app.js` 通过。
- 生产环境人员同步成功，可检索陈鹏列；页面和移动端无控制台错误。
