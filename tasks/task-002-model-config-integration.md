# task-002：模型配置与生产链路接入

- 状态：已部署；待 AI 表权限与模型区域问题处理
- 工作目录：`D:\code_CPL\weekly_report_assistant`
- 交付分支：`feature/task-002-model-config-integration`
- 基线：`origin/main@423a3cdb4347113bb30567366f8baea402674c87`

## 目标

- 增加参考 `bi_center` 的模型配置管理页面，支持 Provider、API Base、模型名、API Key 脱敏、连接测试、保存和恢复部署配置。
- AI 摘要在运行时读取独立服务的生效模型配置，兼容 OpenRouter GPT-5.4、GLM 与 Qwen 的常用参数要求。
- 生产环境复用 `bi_center` 当前的模型配置、员工目录只读 Token 和陈鹏列 operatorId；钉钉使用本项目既有应用与 Secret，不混用 `bi_center` 的机器人身份，不修改 `bi_center` 源码或数据库。
- 保持正式推送人工审核，未完成真实联调前不启用调度器。

## 验收条件

- 模型配置接口只允许管理令牌访问，API Key 永不在读取接口或日志中明文返回。
- 空 API Key 保存时保留当前密钥；恢复操作回到部署环境中同步自 `bi_center` 的配置。
- 连接测试使用候选配置且不必先保存，返回模型、耗时和错误摘要，不返回密钥。
- `readiness.aiSummary` 反映运行时生效配置，而非只检查进程启动时环境变量。
- 单元测试、Python 编译、前端语法和静态契约检查通过。
- 部署后员工目录、AI 表、模型连接和机器人发送能力分别进行无副作用或明确受控的验证。

## 迁移与回滚

- SQLite 继续复用既有 `app_config` 表，不新增数据库表；模型覆盖配置使用独立配置键，旧数据无影响。
- 回滚代码后模型覆盖记录仍保留但不会被旧版本读取；恢复部署配置可删除该配置键。
- 生产 `.env` 仅在服务器本地更新并保持 `600`，不进入 Git。

## 生产验证结果

- 初次功能部署提交：`ee0ee0998241774d91f174dce7cde04e30707b1a`
- `bi_center` 人员目录同步成功：906 人；复用已有只读 Token，旧服务未修改、未重启。
- 钉钉应用鉴权成功；陈鹏列已配置为唯一预览、正式接收人和审核人，群目标为空。
- 机器人 UTF-8 单聊测试成功并取得 `processQueryKey`；发送记录已写入独立服务日志。
- AI 配置已沿用 `bi_center` 的 OpenRouter `openai/gpt-5.4-mini` 和同一把 Key；NeoFlow 中国服务器调用时被上游以区域不可用拒绝（HTTP 403），当前自动降级确定性模板。
- AI 表访问已通过应用凭证阶段，但缺少开放平台权限 `Notable.Base.Read.All`，源表同步被明确拒绝。
- 调度器保持关闭，尚未执行自动或正式周报推送。
- 公网页面、模型配置布局、静态资源、健康检查和浏览器控制台均验证正常。
- 后续可观测性修复将最近源表同步与模型连接测试纳入就绪状态，避免“仅配置齐全”被误判为业务可用。
